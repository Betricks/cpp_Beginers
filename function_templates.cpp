#include<bits/stdc++.h>

using namespace std;

template <typename T>
T maximum(T a, T b){
    return (a > b)? a : b;
} 

 maximum( a,  b){
    return (a, b)? a : b;
}

int main(){
   double  max = maximum(12.0, 22);
    cout << "max : " << max << endl;
    return 0;
}